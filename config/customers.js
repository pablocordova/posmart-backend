var config = new Object();

config.RES = {
  CUSTOMER_IN_SALE: 'Error trying deleting, customer already has sales',
  DUPLICATED_FIRSTNAME: 'Firstname already exist',
  NOT_FOUND: 'Customer does not found'
};

module.exports = config;