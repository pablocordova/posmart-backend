var config = new Object();

config.RES = {
  INVALID_PASSWORD: 'password need more than 1 character',
  INVALID_EMAIL: 'Invalid email',
  NOT_USER: 'User doesnt exits',
  WRONG_PASS: 'Incorrect password'
};

module.exports = config;